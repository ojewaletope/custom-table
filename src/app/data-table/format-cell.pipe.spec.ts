import { FormatCellPipe } from './format-cell.pipe';

describe('FormatCellPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCellPipe();
    expect(pipe).toBeTruthy();
  });
});
